import { test, expect } from "@playwright/test";

//HEADER SECTION IS COMING FROM PLAYWRIGHT.CONFIG.TS FILE
// test("API GET request test", async ({ request }) => {

//   const getResponse = await request.get("https://reqres.in/api/users?page=2");
//   expect(getResponse.status()).toBe(200);

//   const getResponseBody = await getResponse.json();
//   console.log(getResponseBody.data);
//   // Validate that response contains the user email
//   expect(getResponseBody.data[0].email).toBe("michael.lawson@reqres.in");
// });
